{
  "name": "stargaze-portfolio-test",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-content.js",
    "sync": "npm run validate && node scripts/build-projects.js",
    "dev": "npm run sync && vite",
    "build": "npm run sync && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  }
}
