# Backlog

Ideas preserved here are not loaded into the public portfolio. Move a JSON file to `content/sketches/` only when the experiment has a functional prototype ready to publish.

`data-glyphs.json` is the current high-priority data-visualization study.
